import axios from 'axios'
import React from 'react'
import { history } from '../../client/routes/AppRouter'


export const startCreateSub = (sub = {}) => {
  return (dispatch)
  if (sub.name) {
    
  }
}
